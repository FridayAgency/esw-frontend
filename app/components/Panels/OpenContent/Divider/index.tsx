import Container from "@/app/components/Container";

import styles from "./Divider.module.scss";

const Divider: React.FC = () => {
  return <Container className={styles["divider"]}>{null}</Container>;
};

export default Divider;
